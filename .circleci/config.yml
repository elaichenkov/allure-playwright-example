# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1
# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/2.0/configuration-reference/#jobs
jobs:
  playwright-test:
    docker:
      - image: mcr.microsoft.com/playwright:v1.22.0-focal
    environment:
      NODE_ENV: development
    steps:
      - add_ssh_keys:
          fingerprints:
            - "d6:36:58:af:d8:57:47:db:30:77:62:ea:40:f7:03:03"
      - checkout
      - run: npm install
      - run: npm install -g docker 
      - run: docker exec -u root -t -i container_id /bin/bash
      - run: apt-get update
      - run: apt-get -y install sudo
      - run: sudo apt install openjdk-8-jdk -y
      - run: export JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64
      - run: export PATH=$PATH:$JAVA_HOME/bin
      - run: npm i -D playwright @playwright/test
      - run: npm i -D experimental-allure-playwright 
      - run: npm i -D allure-commandline
      - run: npx playwright test --reporter=line,experimental-allure-playwright ||true
      - run: npx allure generate ./allure-results --clean
      - run: npm install gh-pages -g
      - run: git config user.email "anne.h@mailfence.com"
      - run: git config user.name "anneH"
      - run: gh-pages --dist ./allure-report

# Invoke jobs via workflows
# See: https://circleci.com/docs/2.0/configuration-reference/#workflows
workflows:
  e2e-test-workflow:
    jobs:
      - playwright-test
